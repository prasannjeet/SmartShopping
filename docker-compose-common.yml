commandsideservice:
  build: ./cart-command-service/
  #restart: unless-stopped
  ports:
    - 8081:8080

querysideservice:
  build: ./cart-query-service/
  #restart: unless-stopped
  ports:
    - 8082:8080
